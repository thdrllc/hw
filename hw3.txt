#include <stdio.h>
int main(){
    int i,schet,a,c;
char str[50],p;
p='0';
gets(str);
printf("Введенная строка: %s\n",str);
schet=0;
a=strlen(str);
printf("Количество введенных символов: %d\n",a);
if (a>10){
    for (i=0;i<6;i++)
    putchar(str[i]);
}
if (a<=10){
    c=12-a;
    for(i=0;i<a;i++)
    putchar(str[i]);
    for(i=0;i<c;i++)
        putchar(p);
}
}

//Дана строка, если её длина >10, то вывести первые 6 символов. Иначе, дополнить строку символами "0" до длины 12
